import aish.*;
class now
{
	public static void main(String [] now)
{
	System.out.println("in now class");
	matrix only;
	only=new matrix();
	matrix chup=new matrix();
	//chup.set(25,10);
	aish.only.set(25,10);
}
}